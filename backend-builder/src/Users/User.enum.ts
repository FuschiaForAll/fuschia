export enum UserRole {
  ADMIN = "admin",
  USER = "user",
}

export enum UserStatus {
  AWAITING_APPROVAL = "awaiting approval",
  ACTIVE = "active",
  INACTIVE = "inactive",
  SUSPENDED = "suspended",
  BANNED = "banned",
}

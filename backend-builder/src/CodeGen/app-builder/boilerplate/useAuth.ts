export const useAuth = () => {
  return `
import {
  FetchResult,
  gql,
  useApolloClient,
  useMutation,
  useQuery,
} from '@apollo/client'
import { useContext, createContext, useEffect, useState } from 'react'

  `
}
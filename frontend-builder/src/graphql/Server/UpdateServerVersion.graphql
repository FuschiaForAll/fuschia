mutation UpdateServerVersion(
  $projectId: ObjectId!
  $sandbox: Boolean!
  $version: String!
) {
  updateServerVersion(
    projectId: $projectId
    sandbox: $sandbox
    version: $version
  )
}

mutation CreateEntityModel(
  $projectId: ObjectId!
  $name: String!
  $isLocal: Boolean!
) {
  createEntityModel(projectId: $projectId, name: $name, isLocal: $isLocal) {
    _id
    name
    isLocal
  }
}

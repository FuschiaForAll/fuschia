mutation CreateAppVariable(
  $projectId: ObjectId!
  $name: String!
  $type: String!
) {
  createAppVariable(projectId: $projectId, name: $name, type: $type) {
    _id
    name
    type
  }
}

mutation CreateApiVariable(
  $projectId: ObjectId!
  $name: String!
  $type: String!
) {
  createApiVariable(projectId: $projectId, name: $name, type: $type) {
    _id
    name
    type
  }
}

mutation CreateTranslation(
  $projectId: ObjectId!
  $languageId: ObjectId!
  $tagId: ObjectId!
  $translations: [String!]!
) {
  createTranslation(
    projectId: $projectId
    languageId: $languageId
    tagId: $tagId
    translations: $translations
  ) {
    _id
  }
}

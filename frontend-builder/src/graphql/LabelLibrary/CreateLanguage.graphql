mutation CreateLangauge(
  $projectId: ObjectId!
  $languageName: String!
  $languageCode: String!
) {
  createLanguage(
    projectId: $projectId
    languageName: $languageName
    languageCode: $languageCode
  ) {
    _id
  }
}

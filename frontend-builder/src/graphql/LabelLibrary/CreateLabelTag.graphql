mutation CreateLabelTag(
  $projectId: ObjectId!
  $tagName: String!
  $numberOfStates: Int!
) {
  createLabelTag(
    projectId: $projectId
    tagName: $tagName
    numberOfStates: $numberOfStates
  ) {
    _id
  }
}

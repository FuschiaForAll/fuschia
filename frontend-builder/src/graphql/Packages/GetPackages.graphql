query GetPackages {
  getPackages {
    _id
    packageName
    repositoryUrl
    version
    bundle
    components {
      _id
      name
      schema
      defaultValue
      icon
      isRootElement
      isContainer
    }
    authorId
    scope
  }
}

fragment ComponentFragment on Component {
  _id
  package
  type
  name
  x
  y
  isContainer
  isRootElement
  requiresAuth
  parameters {
    _id
    entityType
    path
    label
  }
  fetched {
    entityType
    path
    label
    variables
  }
  props
  data
  parent
}

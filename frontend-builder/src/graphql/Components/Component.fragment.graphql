fragment ComponentFragment on Component {
  _id
  package
  type
  name
  x
  y
  isContainer
  isRootElement
  parameters
  fetched {
    type
    variables
  }
  props
  parent {
    _id
  }
}

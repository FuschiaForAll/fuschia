fragment ComponentFragment on Component {
  _id
  package
  type
  name
  x
  y
  isContainer
  isRootElement
  parameters {
    _id
    entityId
    name
  }
  fetched {
    type
    variables
  }
  props
  data
  parent {
    _id
  }
}

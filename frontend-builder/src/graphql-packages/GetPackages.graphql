query GetPackages {
  getPackages {
    _id
    packageName
    repositoryUrl
    version
    bundle
    components {
      _id
      name
      props
      icon
      isRootElement
    }
    authorId
    scope
  }
}
